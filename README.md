# Poulet Python Library
# How to install
```shell
# Install everything
pip install -U .[all]
# Install editable
pip install -U -e .[all]
# Install specific, choose one or more
pip install -U -e .[hardware,tools,utils]
```
# TODO
